# 第6章 LTspice図 3点監査（2026-07-18）

対象: `flyback_converter.svg`（`book/figures/ltspice/chapter06/`）。監査ツール: `tools/svg_preview.py`
（第5章で回転バグ修正済みのもの、変更なしで流用）。

## 3点監査

### (i) フォントサイズ
部品ラベル・値・ディレクティブは24px統一（既存章と同一）。縮小・潰れなし。

### (ii) はみ出し・重なり
タイトルコメント(y=-120/-88)・回路本体(V1〜R1、制御源V2)・ディレクティブブロック(y=480〜680)
いずれも重なりなし。1次側(V1-S1-L1)と2次側(L2-D1-C1-R1)を横方向に大きく離して配置したため
（`K1 L1 L2 1`ラベルは中間の空白域に単独配置）、既存章のようなタイトル/ディレクティブ衝突は
発生していない。

### (iii) 余白バランス
1次側・制御源V2・2次側の3ブロックが左上・左下・右上に分かれ、右下〜中央下に空白が生じるが、
第5章buckboost_chopperと同程度の密度で過大な偏りではないと判断。

## 監査結果: NGなし。
